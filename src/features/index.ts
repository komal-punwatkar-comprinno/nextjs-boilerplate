/**
 * Barrel file for feature modules.
 *
 * Each feature lives in its own sub-directory and exports its public API
 * from an `index.ts` inside that directory. Re-export those here as they
 * are added.
 */
